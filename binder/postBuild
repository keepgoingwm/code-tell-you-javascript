#!/bin/bash

set -ex
uname -a
# curl -sL https://deb.nodesource.com/setup_12.x | -E bash  - 
# apt-get install -y nodejs npm
# 
npm config set prefix $HOME
npm install -g node-gyp ijavascript itypescript
sudo ~/bin/ijsinstall --install=local
sudo ~/bin/its --install=local
